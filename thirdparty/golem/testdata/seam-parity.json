{
  "note": "Corpus cases where SEAM is knowingly allowed to differ from the legacy engine, each with a reason. The list is a ratchet in both directions: an unlisted difference fails, and a listed case that starts matching fails too. Most differences are now SEAM wins - flows SEAM finds that legacy cannot, because its summaries iterate to a fixpoint over the SCC condensation, because it carries taint through package-level globals, or because its model database covers cgo and template-injection sinks legacy has no rule family for.",
  "cases": {
    "cgo-string-roundtrip/all": "Under --dataflow all SEAM reports three native-interop slices where legacy reports two, one per cgo boundary call rather than collapsing the pair that share a source. The command-execution flow is identical. Same cause as cgo-string-roundtrip/security: SEAM recognises every _Cfunc_ wrapper and models the conversion functions, so it is stricter, not different.",
    "cgo-string-roundtrip/security": "SEAM recognises every cgo wrapper via the _Cfunc_ prefix sink and models the conversion functions, so it reports four slices (one per boundary call plus the command-execution flow); legacy reports three (it has no source for C.GoString and so collapses the duplicate). The shape SEAM reports is correct, just stricter.",
    "context-value/all": "As context-value/security.",
    "context-value/security": "SEAM reports the command-execution flow twice and legacy once. The two slices have different sources for the same data: r.FormValue(\"cmd\") and the *http.Request parameter itself, which SEAM sees through the type-shaped source model and legacy's pattern pack does not reach here. Both routes are real, so neither is dropped; enrichment marks the second with duplicateOf/flowKey, which is the mechanism for collapsing them at presentation time.",
    "deep-chain-8-levels/all": "As deep-chain-8-levels/security.",
    "deep-chain-8-levels/security": "SEAM finds it and legacy does not: SEAM iterates summaries to a fixpoint over the SCC condensation, legacy iterates a fixed four times (defect 11).",
    "defer-taint/all": "As defer-taint/security.",
    "defer-taint/security": "SEAM finds it and legacy does not: SEAM maps a closure's FreeVar indices back to the caller's MakeClosure bindings (so a deferred closure that sinks a captured value is detected), legacy has no equivalent closure-summary application.",
    "dep-only-flow/all": "The flow lies entirely inside a dependency. SEAM summarises reachable dependency functions and now reports the flow from the local caller through the dependency's summary (defect 8 closed: source models no longer fire during summary computation, so param-to-sink summaries are recorded correctly).",
    "dep-only-flow/security": "SEAM finds it and legacy does not: the callee's summary now records the param-to-sink flow (defect 8), and the slice materialises under the local caller's context. Legacy has no summary for the dependency function.",
    "func-field-dispatch/all": "As func-field-dispatch/security.",
    "func-field-dispatch/security": "SEAM resolves a call through a func-typed struct field by indexing every function stored into that field program-wide (funcfield.go); legacy has no equivalent and stops at the field read, so the expectation is marked known-fail=legacy:32.",
    "global-var-carrier/all": "As global-var-carrier/security.",
    "global-var-carrier/security": "SEAM finds it and legacy does not: SEAM has a program-wide global taint map, legacy carries no taint through package-level variables (defect 19).",
    "interface-dispatch/all": "As interface-dispatch/security.",
    "interface-dispatch/security": "SEAM resolves interface dispatch through its implements-index (defect 3 closed); legacy cannot, so the expectation is marked known-fail=legacy:3.",
    "linkname/all": "As linkname/security.",
    "linkname/security": "SEAM finds it and legacy does not: SEAM follows a //go:linkname directive from the body-less declaration to the implementing function, using the mapping the call graph already draws its alias edge from (defect 25 closed for SEAM).",
    "slice-of-pointer/all": "As slice-of-pointer/security.",
    "slice-of-pointer/security": "SEAM finds it and legacy does not: SEAM marks the base allocation tainted when a field is stored, so a returned pointer carries field taint (defect 21).",
    "template-injection/all": "As template-injection/security.",
    "template-injection/security": "SEAM finds it and legacy does not: SEAM has html/template sink models, legacy has no template rule family (defect 24).",
    "worker-pool/all": "As worker-pool/security.",
    "worker-pool/security": "SEAM finds it and legacy does not: SEAM applies a closure's summary at the launch site using the converged block-output state (so a goroutine that receives from a channel sent to later in the same caller is detected), legacy has no equivalent asynchronous summary application (defect 7)."
  }
}
