The wrapper forces --skip-java-db-update, so the Java DB updater never
downloads. Its OCI download path, and go-getter behind it, are not linked.

--- a/github.com/aquasecurity/trivy/pkg/javadb/client.go
+++ b/github.com/aquasecurity/trivy/pkg/javadb/client.go
@@ -18,7 +18,6 @@
 	"github.com/aquasecurity/trivy/pkg/dependency/parser/java/jar"
 	ftypes "github.com/aquasecurity/trivy/pkg/fanal/types"
 	"github.com/aquasecurity/trivy/pkg/log"
-	"github.com/aquasecurity/trivy/pkg/oci"
 )
 
 const (
@@ -101,16 +100,9 @@
 func (u *Updater) downloadDB(ctx context.Context) error {
 	log.InfoContext(ctx, "Downloading Java DB...")
 
-	artifacts := oci.NewArtifacts(u.repos, u.registryOption)
-	downloadOpt := oci.DownloadOption{
-		MediaType: mediaType,
-		Quiet:     u.quiet,
-	}
-	if err := artifacts.Download(ctx, u.dbDir, downloadOpt); err != nil {
-		return xerrors.Errorf("failed to download Java DB: %w", err)
-	}
-
-	return nil
+	// trivy-cdxgen: the OCI downloader is not linked. The wrapper forces
+	// --skip-java-db-update, so Update never reaches this point.
+	return xerrors.New("downloading the Java DB is not supported by trivy-cdxgen")
 }
 
 func Init(cacheDir string, javaDBRepositories []name.Reference, skip, quiet bool, registryOption ftypes.RegistryOptions) {
