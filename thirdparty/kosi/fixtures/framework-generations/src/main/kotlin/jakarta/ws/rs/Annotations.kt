// The jakarta generation of the same JAX-RS surface, so one fixture pins
// BOTH generations of every routing symbol against the same detector.
package jakarta.ws.rs

annotation class GET
annotation class Path(val value: String)
annotation class Consumes(val value: String)
annotation class Produces(val value: String)
