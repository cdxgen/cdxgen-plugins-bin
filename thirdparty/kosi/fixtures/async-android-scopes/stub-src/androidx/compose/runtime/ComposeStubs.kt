// androidx.compose.runtime.LaunchedEffect, the compose-runtime 1.6.0 shape
// (see ../androidx/lifecycle/LifecycleStubs.kt for the sourcing note).
package androidx.compose.runtime

import kotlinx.coroutines.CoroutineScope

fun LaunchedEffect(key1: Any?, body: suspend CoroutineScope.() -> Unit) {}
